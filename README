Chess
========

[Description]
This project is the implementation of a chess game.

How to launch
-------------
If the executable 'chess' does not exist :  
    `.configure`  
    `make`  

Then,  
    `./chess [--player-white=[human | *.so] --player-black=[human | *.so ]]
             [*.so ...] [file.pgn]`
